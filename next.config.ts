import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  
};

module.exports = {
  images: {
    domains: ['img.daisyui.com'],  // Agrega aquí el dominio de las imágenes externas
  },
};


export default nextConfig;
